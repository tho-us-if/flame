name: flame_devtools
description: "The DevTools extension for Flame"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  devtools_extensions: ^0.0.10
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flame_lint: ^1.1.2

flutter:
  uses-material-design: true
